import 'package:flutter/material.dart';

class SelectFromGallaryOrCamera extends StatelessWidget {
  const SelectFromGallaryOrCamera({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
